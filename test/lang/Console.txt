namespace lang;

public final class Console {

    public static void Write(string value) {
        native (source) {
            system::console::write(f_value.data);
        }
    }

    public static void Write(let Object value) {
        print(value == null ? "null" : value.toString());
    }

    public static void WriteLine(string value) {
        native (source) {
            system::console::writeline(f_value.data);
        }
    }

    public static void WriteLine(let Object value) {
        println(value == null ? "null" : value.toString());
    }

    public static string ReadChar() {
        native (return) {
            return lang_string(system::console::readchar());
        }
    }

    public static string ReadLine() {
        native (return) {
            return lang_string(system::console::readline());
        }
    }
}