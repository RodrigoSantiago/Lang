namespace lang;
using lang::Locale.GlobalLocale;

void main () {
}

public class Test {

    public this() {
    }

    public void letsYield(Object work) {
        function<Iterator<int>> doYield = () -> {
            for (int b = 0; b < 100; b++) {
                if (null == work) yield break;
                yield return 10;
            }
        };
    }
}